        //veeva approved email with fragments

        function addfragment_fu(id) {
            $msg="fragment "+id+" added";
            alert ($msg);
        }

        function SendVeevaApprovedEmail_fu() {
            alert ("send email");
        }
